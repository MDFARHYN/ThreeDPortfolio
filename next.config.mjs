/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['assets.aceternity.com','images.unsplash.com','via.placeholder.com','avatar.vercel.sh'], // Add the hostname here
      },
};

export default nextConfig;
